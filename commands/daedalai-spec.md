---
description: "Draft and attach a SPEC document to a DaedalAI work item — Problem / Approach / Verification / Risks — then flip hasSpec=true."
---

Invoke the `daedalai-create-spec` skill with the arguments the user
provided. Expected format: `<workItemKey> [one-paragraph summary]`.
The skill will synthesize from existing context if the summary is
absent.

Arguments: $ARGUMENTS
